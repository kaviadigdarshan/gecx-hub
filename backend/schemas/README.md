# GECX Schema Registry
scaffold_context.schema.json — canonical ScaffoldContext contract (v1.0).
All accelerators that read/write ScaffoldContext must validate against this schema.
Bump schema_version in ScaffoldContext when adding new top-level fields.
